{
  "name": "projects-picker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Seletor TUI (Ink/React) de projetos em ~/Projects — mais recente primeiro, filtro por digitação",
  "bin": {
    "projects-picker": "index.js"
  },
  "dependencies": {
    "ink": "^5.2.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "react-devtools-core": "^7.0.1"
  },
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --platform=node --format=esm --packages=external --banner:js=\"#!/usr/bin/env node\" --outfile=dist/index.js"
  }
}
